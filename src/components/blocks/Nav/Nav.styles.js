import styled from 'styled-components';

export const StyledNav = styled.nav`
	display: flex;

	& > a {
		display: block;
		padding: 0.5em;
		color: #969696;
		text-decoration: none;
		font-size: 0.9em;
		cursor: pointer;
	}
`;